---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    <Header />
    <main class="layout">
      <aside class="sidebar">
        <h2 class="name">Anastasia Psarou</h2>
        <p class="email">
          <a href="mailto:anastasia.psarou@uj.edu.pl">anastasia.psarou@uj.edu.pl</a>
        </p>
        <img
          src="/images/anastasia.jpeg"
          alt="Photo of Anastasia Psarou"
          width="200"
          height="200"
          class="avatar"
        />
        <p class="tags">Multi-agent systems · Reinforcement learning · Agent-based modeling · Transportation</p>
      </aside>

      <section class="content">
        <p>
          <strong>Current role:</strong> I am a second-year PhD student at the
          <a href="https://www.uj.edu.pl/">Jagiellonian University</a>, working under the supervision of
          Professor <a href="https://www.rafalkucharskilab.pl/">Rafał Kucharski</a>.
          My research is conducted within the
          <a href="https://github.com/COeXISTENCE-PROJECT">COeXISTENCE</a> project, funded by the European Research Council (ERC).
        </p>

        <p>
          <strong>Research:</strong> My work investigates how autonomous vehicles make routing decisions.
          I develop multi-agent reinforcement learning models to simulate and optimize their interactions within dynamic traffic networks.
        </p>

        <h3 class="section-title">News</h3>
        <ul class="news">
          <li>
            I am currently undertaking a six-month research visit at
            <strong>ETH Zurich</strong> within the
            <strong>Institute for Transport Planning and Systems (IVT)</strong>.
            My work focuses on congestion pricing and fairness in multi-agent traffic systems,
            leveraging multi-agent reinforcement learning methods. The visit is jointly supervised by
            <a href="https://www.ivt.ethz.ch/en/people/profile.anastasios-kouvelas.html">Dr. Anastasios Kouvelas</a>
            and
            <a href="https://www.michailmakridis.com/">Dr. Michalis Makridis</a>,
            and supported by the “Excellence Initiative – Research University” grant of Jagiellonian University.
          </li>

          <li>
            Our paper
            <a href="https://example.com">
              <em>URB -- Urban Routing Benchmark for RL-equipped Connected Autonomous Vehicles</em>
            </a>
            got accepted to NeurIPS 2025 in San Diego.
          </li>

          <li>
            Our paper
            <a href="https://www.sciencedirect.com/science/article/pii/S2352711025002468">
              <em>RouteRL: Multi-agent reinforcement learning framework for urban route choice with autonomous vehicles</em>
            </a>
            got published in <em>SoftwareX</em> journal.
          </li>
        </ul>
      </section>
    </main>
    <Footer />

    <style>
      /* Layout */
      .layout {
        --rail-min: 120px;       /* smallest left column on tiny screens */
        --rail-max: 240px;       /* cap the left column on wide screens  */
        --gap: 1.25rem;

        max-width: 1100px;
        margin: 0 auto;
        padding: 2rem 1rem;
        display: grid;
        grid-template-columns: minmax(var(--rail-min), var(--rail-max)) 1fr;
        gap: var(--gap);
        align-items: start;
      }

      /* Left column */
      .sidebar {
        position: sticky;
        top: 1rem;
      }

      .name {
        margin: 0 0 0.25rem 0;
        font-size: clamp(1.2rem, 2.2vw, 1.8rem);
        line-height: 1.2;
      }

      .email {
        margin: 0 0 0.75rem 0;
        word-break: break-word;
        font-size: 0.95rem;
      }

      .avatar {
        display: block;
        width: 100%;
        height: auto;
        border-radius: 8px;
        margin: 0 0 0.75rem 0;
        object-fit: cover;
      }

      .tags {
        font-size: 0.92rem;
        color: var(--text-dim, #555);
      }

      /* Right column */
      .content p {
        margin: 0 0 1rem 0;
        line-height: 1.65;
        font-size: 1.02rem;
      }

	  .news li {
		font-size: 1.02rem;   /* match .content p */
		line-height: 1.65;
		}


      .section-title {
        margin: 1.25rem 0 0.5rem 0;
        font-size: 1.1rem;
      }

      .news {
        margin: 0 0 1rem 1.1rem;
        padding: 0;
      }

      .news li + li {
        margin-top: 0.6rem;
      }

      /* Tablet adjustments */
      @media (max-width: 900px) {
        .layout {
          padding: 1.5rem 1rem;
          --rail-max: 200px;
          --gap: 1rem;
        }
        .avatar {
          max-width: 180px;
        }
      }

      /* Keep two columns on phones, but tighten spacing and shrink the left rail */
      @media (max-width: 640px) {
        .layout {
          --rail-min: 96px;
          --rail-max: 140px;
          --gap: 0.75rem;
        }
        .sidebar {
          position: static; /* avoid sticky jumpiness on small screens */
        }
        .avatar {
          max-width: 120px;
          border-radius: 6px;
        }
        .name { font-size: 1.05rem; }
        .email, .tags { font-size: 0.88rem; }
        .content p, .news { font-size: 0.98rem; }
      }

      /* Ultra-small devices */
      @media (max-width: 380px) {
        .layout {
          --rail-min: 84px;
          --rail-max: 110px;
          padding: 1rem 0.75rem;
        }
        .avatar { max-width: 100px; }
        .news { margin-left: 0.9rem; }
      }

      /* Respect user's reduced motion preference */
      @media (prefers-reduced-motion: reduce) {
        * { scroll-behavior: auto !important; }
      }

	/* 1) Make the page/base background white */
	html, body {
	background: #fff;
	}

	/* 2) Ensure the left rail has no grey background */
	.sidebar,
	.layout > .sidebar {
	background: transparent !important;
	box-shadow: none !important;
	}

	/* 3) Keep two columns on phones, but cap the left rail so it never looks like a big grey block */
	@media (max-width: 640px) {
	.layout {
		grid-template-columns: 110px 1fr; /* narrow left rail + flexible right */
		gap: 0.75rem;
	}
	.sidebar {
		position: static; /* avoid sticky jumpiness on mobile */
	}
	.avatar { max-width: 110px; }
	}

	/* 4) Header: give it a solid background + better contrast for links */
	header, .site-header, header[role="banner"] {
	position: sticky;   /* optional; remove if not desired */
	top: 0;
	z-index: 100;
	background: #fff;   /* ensures links sit on white, not over content behind */
	border-bottom: 1px solid rgba(0,0,0,0.06);
	}

	/* Make nav items wrap and be readable */
	header nav, .site-nav, header .nav {
	display: flex;
	flex-wrap: wrap;
	gap: 0.75rem;
	align-items: center;
	}

	/* Increase contrast for header links */
	header a, header nav a, .site-header a {
	color: #111 !important;         /* visible on white */
	text-decoration: none;
	font-weight: 500;
	}
	header a:visited { color: #111 !important; }

	/* Slightly smaller links on very small screens so they fit */
	@media (max-width: 480px) {
	header nav a, .site-header a { font-size: 0.95rem; }
	/* If your nav is wide, allow horizontal scroll instead of wrapping to a second line */
	header nav { overflow-x: auto; -webkit-overflow-scrolling: touch; }
	}

    </style>
  </body>
</html>
